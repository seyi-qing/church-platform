# Mux livestream setup

1. Create a Mux account and get Token ID + Secret.
2. Set in `.env`:

```
MUX_TOKEN_ID=...
MUX_TOKEN_SECRET=...
```

3. Create a session from Admin → Livestream.
4. Use the stream key with OBS or similar.
5. When not configured, paste a YouTube live URL instead.
